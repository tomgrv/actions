{
    "name": "run-pint",
    "version": "2.20.0",
    "description": "Run Laravel Pint with reviewdog.",
    "inputs": {
        "github-token": {
            "description": "GitHub token for reviewdog reporting.",
            "required": false,
            "default": "${{ github.token }}"
        },
        "paths": {
            "description": "Comma-separated list of paths to analyze with Pint. Defaults to `app`.",
            "required": false,
            "default": "app"
        },
        "preset": {
            "description": "Pint preset to use (e.g. laravel, default, etc.). Defaults to `laravel`. Ignored when `config` is set.",
            "required": false,
            "default": "laravel"
        },
        "config": {
            "description": "Path to a custom Pint configuration file. Leave empty to let Pint auto-detect `pint.json` at the repository root, or fall back to `preset` otherwise.",
            "required": false,
            "default": ""
        },
        "blade": {
            "description": "Format `.blade.php` files too (Pint's `--blade` flag, added in Pint 1.30). Requires `prettier`, `prettier-plugin-blade` and `prettier-plugin-tailwindcss` available in the repository.",
            "required": false,
            "default": "false"
        },
        "dirty": {
            "description": "Only analyze files with uncommitted git changes (staged, unstaged, or untracked).",
            "required": false,
            "default": "false"
        },
        "wip": {
            "description": "Only analyze files changed on the current pull request (relative to its base branch).",
            "required": false,
            "default": "false"
        },
        "wip-base-ref": {
            "description": "Base branch/ref to diff against when `wip` is enabled. Defaults to `GITHUB_BASE_REF`, which GitHub Actions sets automatically on `pull_request` events.",
            "required": false,
            "default": ""
        },
        "name": {
            "description": "Name reported by reviewdog to identify this check.",
            "required": false,
            "default": "pint"
        },
        "level": {
            "description": "Report level for reviewdog [info,warning,error].",
            "required": false,
            "default": "error"
        },
        "reporter": {
            "description": "Reporter of reviewdog command [github-pr-check,github-check,github-pr-review]. Defaults to the reporter resolved by setup-reviewdog for this run's context (github-pr-check on pull requests, github-check otherwise).",
            "required": false,
            "default": ""
        },
        "fail-level": {
            "description": "Exit code for reviewdog if it finds at least the specified level of diagnostic [none,any,info,warning,error].",
            "required": false,
            "default": "none"
        },
        "reviewdog-flags": {
            "description": "Additional reviewdog flags.",
            "required": false,
            "default": ""
        }
    },
    "private": true
}
