# @format

name: Validate PR Source Branch

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read
  pull-requests: write

jobs:
  validate-source:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR source branch
        uses: tomgrv/actions/check-pr-source-branch@v2
