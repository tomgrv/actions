{
    "name": "run-filacheck",
    "version": "2.30.0",
    "description": "Run FilaCheck with reviewdog.",
    "inputs": {
        "github-token": {
            "description": "GitHub token for reviewdog reporting.",
            "required": false,
            "default": "${{ github.token }}"
        },
        "FILACHECK_PATH": {
            "description": "Path to analyze with FilaCheck. Defaults to `app/Filament`.",
            "required": false,
            "default": "app/Filament"
        },
        "DETAILED": {
            "description": "Show detailed output with rule categories.",
            "required": false,
            "default": "false"
        },
        "DIRTY": {
            "description": "Only scan files with uncommitted git changes.",
            "required": false,
            "default": "false"
        },
        "WIP": {
            "description": "Only scan files changed on the current pull request (relative to its base branch). FilaCheck has no native flag for this, so it is emulated by resolving the changed files and passing that explicit list in place of `path`.",
            "required": false,
            "default": "false"
        },
        "wip-base-ref": {
            "description": "Base branch/ref to diff against when `wip` is enabled. Defaults to `GITHUB_BASE_REF`, which GitHub Actions sets automatically on `pull_request` events.",
            "required": false,
            "default": ""
        },
        "REVIEWDOG_NAME": {
            "description": "Name reported by reviewdog to identify this check.",
            "required": false,
            "default": "filacheck"
        },
        "REVIEWDOG_LEVEL": {
            "description": "Report level for reviewdog [info,warning,error].",
            "required": false,
            "default": "error"
        },
        "REVIEWDOG_REPORTER": {
            "description": "Reporter of reviewdog command [github-pr-check,github-check,github-pr-review]. Defaults to the reporter resolved by setup-reviewdog for this run's context (github-pr-check on pull requests, github-check otherwise).",
            "required": false,
            "default": ""
        },
        "REVIEWDOG_FAIL_LEVEL": {
            "description": "Exit code for reviewdog if it finds at least the specified level of diagnostic [none,any,info,warning,error].",
            "required": false,
            "default": "none"
        },
        "REVIEWDOG_FLAGS": {
            "description": "Additional reviewdog flags.",
            "required": false,
            "default": ""
        }
    },
    "private": true
}
