{
    "name": "list-packages",
    "version": "2.29.0",
    "description": "Discovers Composer and npm workspace packages and emits a JSON matrix array.",
    "inputs": {
        "workdir": {
            "description": "Working directory to search for packages. Defaults to the repository root.",
            "required": false,
            "default": "${{ github.workspace }}"
        },
        "filter": {
            "description": "A jq `select()` boolean expression evaluated against each final package object (e.g. '.private == false', '.registry.npmjs.published == false'). Only packages for which it evaluates truthy are kept. Leave unset (default) to keep all packages.",
            "required": false,
            "default": ""
        },
        "require-repository": {
            "description": "When 'true', drop packages whose repository is not defined before applying `filter`. Set to 'false' to keep packages without a repository.",
            "required": false,
            "default": "true"
        }
    },
    "outputs": {
        "packages": {
            "description": "JSON array of package objects (org, name, path, repository, private, registry)."
        }
    },
    "private": true
}
